// @txwhy/harvester — pages busy programs for failed transactions, saves fixtures.
// Implemented in the harvester milestone; committed as a reusable tool, not a one-off.
console.error("harvester: not implemented yet");
process.exit(1);
